import React from 'react'

const Title = (props) => {
    return (
        <div>
            <h1 className="d-flex justify-content-center my-5">{props.title}</h1>
        </div>
    )
}

export default Title
